coverage:
  status:
    project:
      default:
        target: 85%
        threshold: 1%
    patch:
      default:
        target: 80%

comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: false
